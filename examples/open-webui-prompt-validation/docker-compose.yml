services:
  open-webui:
    container_name: open-webui
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    env_file:
      - local.env
    volumes:
      - /app/backend/data
    restart: always
  
